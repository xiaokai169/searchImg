# ========================================
# 以图搜图 v2.0 — CLIP + ResNet50 双模型
# 4核服务器，1万图片，零外部依赖
# ========================================

# Web 框架
flask>=3.0
requests>=2.28

# 向量检索引擎
faiss-cpu>=1.7.4

# 图像处理
Pillow>=10.0
numpy>=1.24

# ONNX 推理（运行时使用 ONNX 模型，不需要 PyTorch）
onnxruntime>=1.16

# ========== 以下仅用于下载/导出模型（可选） ==========
# torch>=2.0
# torchvision>=0.15
# open-clip-torch>=3.0
